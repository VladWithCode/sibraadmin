import { redTypes } from "../types/reduxTypes";


const initialState = {

}

const recordReducer = (state = initialState, action) => {

    switch (action.type) {
        case redTypes:

            break;

        default:
            break;
    }

}